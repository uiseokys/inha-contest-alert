const test=require('node:test'),assert=require('node:assert/strict'),R=require('../web/runtime.js'),P=require('../web/personal.js');
test('stale comparisons clear current new ids and counts',()=>{const c=R.comparison({status:'comparable',as_of:'2026-09-23T12:00:00+09:00',new_count:2,new_ids:['a'],new_event_count:1,new_event_ids:['evt_a'],by_source:[{count:2}]},new Date('2026-09-24T01:00:00+09:00'));assert.equal(c.status,'stale');assert.equal(c.new_count,null);assert.deepEqual(c.new_ids,[]);assert.equal(c.new_event_count,null);});
test('comparison timestamp uses Korea date even if saved offset differs',()=>{const c=R.comparison({status:'comparable',as_of:'2026-09-22T15:01:00Z'},new Date('2026-09-23T00:05:00+09:00'));assert.equal(c.status,'comparable');});
test('normalized foreign instant takes priority over Korean date',()=>{const i={deadline:'2026-09-22',deadline_time:'23:00',registration_timezone:'America/New_York',deadline_at:'2026-09-23T12:00:00+09:00'};assert.equal(R.status(i,new Date('2026-09-23T11:59:00+09:00')),'active');assert.equal(R.status(i,new Date('2026-09-23T12:00:00+09:00')),'closed');});
test('ambiguous cached deadline sorts with unknown',()=>{assert.ok(R.compareItems({title:'old',deadline:'2026-12-01',registration_ambiguous:true},{title:'confirmed',deadline:'2026-10-01'},'remaining',new Date('2026-09-23T12:00:00+09:00'))>0);});
test('known time sorts before date-only on the same day',()=>{assert.ok(R.compareItems({title:'Z',deadline:'2026-10-01',deadline_time:'18:00'},{title:'A',deadline:'2026-10-01'},'remaining',new Date('2026-09-23T12:00:00+09:00'))<0);});
test('successful write restores availability after corrupted read',()=>{let value='{';const s=P.store({getItem(){return value},setItem(k,v){value=v}},'key');s.read();assert.equal(s.available,false);s.write(P.defaults());assert.equal(s.available,true);});
