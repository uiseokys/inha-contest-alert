# v7 progress
Baseline: supplied v6 archive. Original 190 Python tests passed.
Read-only web verification: live JSON now shows parser version 6, collected 2026-09-22 23:54 KST; it is newer than the previous review. Raw Python HTTPS failed DNS in this environment. No external writes/notifications.
Ruling: user approved implementing all four priorities; retain prior approved design and execute directly rather than request further design confirmations.
Ruling: favorites are browser-local with manual export/import. Cross-device backend sync and favorite-specific cloud pushes are not implied.
Priority 1: Python regression RED (8 assertions), then 202/202 GREEN. JS runtime assertions RED before helper, then 5/5 GREEN. Exact timezone-aware state, date-only labels, build/collection metadata, ntfy post-send rebuild, workflow persistence of updated website added. Browser metadata and clock rollover now integrated; full browser pass deferred until all UI features are connected.
Priority 2: 13 new regression tests initially had 12 failing assertions; primary-window extraction, field-content hashing, request-stage tracing, evidence-required date corrections, diagnostic report and read-only GitHub audit workflow implemented. Full suite reached 215/215; additional denominator/privacy regressions added below. No new paid API or runtime dependency. PDF/HWP/OCR is deliberately not represented as implemented.
Ruling: operation source reports label their denominator as saved relevant notices (including expired). They separately count detail pages attempted/confirmed in this run; cached dates must not be misreported as freshly extracted.
Priority 3: conservative official-link/exact-evidence event grouping, stable raw-ID registry and favorite aliases, repost-vs-new-event counts, extension changes, grouped dashboard/README/CSV/digest completed. 14 new Python regressions failed before changes, full suite 232/232 GREEN. Original notice records/snapshots/claims preserved; public event export is field-whitelisted. Frontend acceptance will exercise the connected P3+P4 UI.
Priority 4: independent technical-field tags/filter, browser-local favorites, persisted filters, bounded JSON export/import with alias migration, urgent favorites strip, separate review-pending section and per-source diagnostic UI completed. Eight Python tests and the personal helper test failed before implementation; full Python suite 240/240 and Node 18/18 passed. Browser UI missing-field assertion failed first; connected checks passed at 320/390/768/1440px.
Ruling: browser navigation is blocked by this harness even for localhost/route-fulfilled URLs. DOM tests use set_content and a Storage-compatible injected adapter to exercise preference persistence across new documents. They do not prove native origin-backed localStorage or remote Pages behavior. The real code uses localStorage and tested memory fallback if unavailable. Asynchronous file-import assertion was changed to wait for completed File.text(), not asserted before the browser event finished.

Final review: self-review (no independent reviewer tool). Separate post-implementation review added regressions for public-field whitelisting, UI/workflow fingerprint coverage, explicit timezone crossings, same-day clock sorting, invalid/ambiguous date handling, stale badges and UTC/KST comparison dates, manual-vs-automatic coverage, storage failures, named tracks, parser correction vs formal extension, and noon-target digest timing. All fixes passed the full suite: 254 Python / 24 Node. No real notification or remote deployment.
Ruling: public API lookup is optional, unauthenticated and bounded. Failure/rate limit never implies successful deployment; program-start failures still require workflow logs.
Ruling: date extensions inferred from stored changes require original-source verification. Parser-version changes are labelled extraction corrections rather than announced as official extensions.

Package gate: clean full archive and original-v6 merge each passed Python 254/254 and Node 24/24, syntax/render checks. Preserved state/config/README/active override bytes at patch time; preserved original IDs/claims/prior snapshots after grouping. Workflow replacement checked separately. Both ZIP CRC/font/bytecode checks passed.
