# v6 검증 보고서 — 실제 운영 사례 점검

검증일: 2026-09-22. 아래 결과는 공개 웹 조사 및 로컬 코드·화면 검사입니다. 원격 배포/ntfy 수신 성공 확인서가 아닙니다.

## 조사 범위

사용자가 제공한 Pages, 공개 GitHub main의 README, site/data.json, data/state.json 및 배포 코드·workflow를 웹 도구로 확인했습니다. 조사한 공개 snapshot의 수집 시각은 2026-09-22T16:18:23.684311+09:00, 공개 항목은 23개였습니다. 브라우저가 JS를 실행하기 전의 HTML 기본 ‘0건’은 데이터 부재의 근거로 삼지 않았습니다.

실제 원문으로 DACON 236754/236749/236753 일정 탭, AI Factory 9306/9307을 확인했습니다. 두 DACON 원문에서 대회 종료연도 2025가 시작연도 2026보다 앞서는 오류는 임의로 바로잡지 않고 행사기간을 불확정 처리하며, 별도로 명시된 참가기간은 보존합니다.

## 실제 결과

| 검사 | 결과 |
|---|---|
| 제공 v5 기준 Python suite | 165개 통과 |
| v6 전체 Python suite | **190개 통과**, 실패 0 |
| 새 회귀 테스트 | 25개 — URL 중복/기록 보존, 모집·결과 구별, 일정 라벨/별도 탭, 약관 오인식, 제목 연도 근거, 조회 대기 표시 등 |
| Python compileall | 통과 |
| JavaScript node --check | 통과 |
| 선택한 실제 사례 재현 화면 | Chromium 320/390/768/1440px, 오류 0 |
| 기존 검색·필터·CSV | 기존 데스크톱/휴대폰 브라우저 검사 통과 |
| 기존 상세정보·원문 링크 | 기존 데스크톱/휴대폰 브라우저 검사 통과 |
| 기존 v5 정책·제목 복구 화면 | 320/390/768/1440px 통과 |
| 비동기 공개 페이지 준비 대기 | 행사기간 배너만 읽고 끝내지 않고 늦게 렌더링된 참가접수 일정을 기다리는 검사 통과 |
| 화면 직접 검토 | 실제 사례 재현의 데스크톱/휴대폰 스크린샷 검토 |

`verification/v6/`에는 실제 테스트 출력을 포함합니다. 새 테스트에서 기존 구현의 실패를 확인한 뒤 수정했습니다. 마지막 자체 검토에서는 보험사 이름의 ‘손해’를 약관으로 잘못 제외하는 조건과 주제형 AF 제목의 종류 오분류를 별도 실패 테스트로 재현한 뒤 수정했습니다. 독립 검토자가 아닌 작성자의 자체 검토입니다.

## 재현 자료의 의미

`tools/preview_audit_v6.py`의 제목/주소는 사용자의 공개 snapshot에서 선택한 사례입니다. 참가기간·시각은 직접 읽은 공식 원문을 바탕으로 최소 HTML로 재현합니다. **원본 HTML 바이트를 그대로 다운로드한 파일도, 운영 state 전체를 재실행한 결과도 아닙니다.**

공식 일정 재현 5개 대회의 시작일·마감일·시각이 추출되고, 제목의 연도/기간이 명확한 지난 신청 2건이 마감 상태가 되는 것을 검사합니다. 결과 안내 중복은 숨깁니다. 캠퍼스픽 1개와 세미나는 원문 날짜를 확인하지 못했으므로 미확인으로 남깁니다. 이전날 전체 기록을 옮기지 않았으므로 이 미리보기의 신규 비교는 준비 중입니다.

## 한계

제작 컨테이너의 외부 DNS 요청은 실패했습니다. Python 수집기로 실제 사이트를 요청하는 종단간 검증, 실사용 GitHub Actions 실행, Pages 실제 배포, ntfy 전송·휴대폰 수신은 **하지 않았습니다**. 공식 웹 원문을 확인한 사실과 GitHub runner에서 수집이 성공하는 것은 다릅니다.

사이트 구조·robots 정책·로그인·접속 제한·요청 예산에 따라 날짜가 남아 있을 수 있습니다. 캠퍼스픽은 공개 웹 읽기에서 제목 외 본문을 얻지 못한 사례가 있습니다. PDF/HWP/포스터 OCR은 추가하지 않았습니다. 같은 게시글의 URL 별칭은 통합하지만 서로 다른 플랫폼의 같은 대회까지 의미적으로 모두 합치지 않습니다. 날짜별 최대 한 번 전송 시도 정책과 정오 지연 가능성은 동일합니다.

## 다시 검사

```bash
python -m unittest discover -s tests -v
python -m compileall -q contest_alert tests tools
node --check web/app.js
python tools/preview_audit_v6.py --output preview-v6.html
python tools/check_v6_browser.py preview-v6.html --screenshots screenshots
python tools/check_render_wait_v6.py
```

브라우저 검사 도구는 시스템 Chromium이 설치된 로컬 개발 환경용입니다. 사용자에게 Chromium을 따로 설치해 계속 PC에서 실행하라는 뜻은 아닙니다. 배포 workflow의 기존 Playwright 설치는 그대로 유지합니다.

## 업데이트 묶음 검사

배포 전 전체 ZIP을 별도 폴더에서 검사하고, 업데이트 ZIP을 제공된 원본 v5의 별도 복사본에 적용해 확인합니다. 검증용 state의 공고 ID·중복·발송 기록·전일 snapshot과 config/workflow/README 표식을 넣고 **업데이트 직후 보호 파일의 바이트 동일성**, 이후 정책 재적용의 기록 보존과 공개 중복 숨김을 검사합니다. 이 과정은 운영 저장소나 실사용 state를 쓰지 않습니다. 결과는 `verification/v6/package-validation.txt`에 기록합니다.
