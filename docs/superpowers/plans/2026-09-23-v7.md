# Contest Monitor v7 Implementation Plan

**Goal:** Implement the approved four priorities in sequence without resetting operational data.
**Architecture:** Pure time/health/date-quality/event identity functions feed the same public rendering and notification paths. Browser-only preferences remain outside server state.
**Tech Stack:** Existing Python/BeautifulSoup/Requests/Playwright and vanilla JavaScript, GitHub Actions/Pages and ntfy.
**Spec:** docs/superpowers/specs/2026-09-23-v7.md

## Global constraints
Asia/Seoul; Python 3.12+; free-host defaults; existing ID/claim/snapshot preservation; no topic/credential in public output; no real notification during verification.

## Review focus
- Known deadline minute and date-only deadlines on browsers outside Korea.
- Cross-platform same-title but different track/year/round, later merge and favorite continuity.
- All-failed collection, stale midnight comparisons and ntfy accepted vs delivered distinction.
- Invalid correction/import payload, HTML/CSV injection and disabled localStorage.
- Applying the update ZIP to an already populated v6 repository, including required workflow changes.

## 1. Exact clock and operations
- [x] Write regression tests in tests/test_v7_time_ops.py using existing status_of/public_data; run `python -m unittest discover -s tests -p test_v7_time_ops.py -v` and observe failing assertions.
- [x] Implement contest_alert/timing.py and operations.py; integrate core, render, CLI and workflow final rendering. Exports: status_at(item, now), timing_label(item, now), runtime_metadata(state, now).
- [x] Add pure web/runtime.js clock/status helpers and node tests. Render embeds helper before app.js.
- [x] Run full Python suite and JS tests, commit and record exact output.

## 2. Measured extraction quality
- [x] Write tests/test_v7_dates_quality.py for scholarship/refund boundaries, evidence-required corrections and error/budget reason classification; observe failures.
- [x] Improve details entries/extraction, add diagnostics.py and corrections.py with `diagnostic_report(state, now)`, `apply_corrections(state, config, now)`. Integrate bounded per-record stage metrics and workflow summary/audit.
- [x] Run full suite; preserve automatic dates alongside corrections; record verified scope and commit.

## 3. Event identity and fresh comparison
- [x] Write tests/test_v7_events.py for shared official URL, different-year/round/track rejection, stable alias/new-group comparisons, and deadline-extension digest.
- [x] Implement events.py with `group_events(state)` and `event_comparison(state, comparison, events)`. Raw IDs remain; rendering displays merged source links. Integrate digest and daily date-aware browser labels.
- [x] Run full suite and commit.

## 4. Personal finding
- [x] Write technical-field classification tests and browser test tools/check_v7_browser.py for favorites, filters, JSON import/export, midnight/time changes, no-network storage and focus.
- [x] Implement taxonomy.py, web/personal.js and app UI, preserving existing tokens and farthest-first. Bounded JSON validation, localStorage fallback, pending view and urgent favorites.
- [x] Run Python, node and Chromium tests; inspect screenshots; commit.

## Finish
- [x] Separate self-review pass (no independent reviewer tool). Fix important findings with new failing tests and full suite.
- [x] Generate full and update archives. Test fresh install and merge into populated v6; verify state/config/README preservation and actual workflow replacement.
- [ ] Deliver updated files, preview, precise verification results and deployment instructions; no claim of remote deployment.
