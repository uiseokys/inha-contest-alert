# v7 — approved priorities 1 → 4

## User intent and invariants
Implement the four priorities approved in chat, in order, on the supplied v6 project. Keep free public GitHub Actions/Pages, ntfy one daily submission attempt targeting 12:00 Asia/Seoul, farthest-deadline-first ordering, the existing design tokens, source exclusions, and all persistent notice IDs/claims/snapshots. Do not publish, connect a shared account to personal credentials, or send notifications from this development environment.

## 1. Operations and exact time
Use aware Korea time for known registration start/deadline times. At the deadline instant the item is closed. Missing times stay unknown, not invented as 23:59. Refresh browser time-derived state without a collection; preserve keyboard focus/open details. Publish whitelisted code/build/collection/notification metadata; distinguish partial/full success and HTTP acceptance from phone delivery. After sending, rebuild the website before its artifact is uploaded. Show the actual Actions run link. A browser-side read of public Actions status may supplement the last saved state, never pretend a failed fetch means success.

## 2. Date quality
Record per-source date denominator/coverage and per-item reason (not requested, budget deferred, access/robots failure, render failure, conflict, no explicit dates, confirmed). Produce a small public diagnostic report from each real run and a manual audit workflow for GitHub execution. Add regression tests for the observed AI-POT primary-registration/scholarship/refund conflict. Do not claim live collector success from synthetic fixtures. Optional administrator corrections require evidence URL, confirmation date and reason, preserve raw automatic values, and signal later conflicting observations. Bound all network work. No login bypass, blanket OCR, new paid API or large language model.

## 3. Identity and comparison
Group only when a shared official event-specific URL or a rigorously matching year/title/organizer/period identifies the same opportunity. Distinct years/rounds/tracks or official IDs must not merge. Preserve raw notices and their source links, use stable group identities, retain favorite aliases when merging. Compare both raw newly seen notices and distinct new opportunities against yesterday's stored IDs. A newly discovered repost of an old event is not a new event. Separate imported sources and incomplete observation. Browser date rollover must not keep an old 'yesterday' label. Digest uses the same grouping/counts and highlights confirmed deadline extensions.

## 4. Finding and saving
Add technical-field filter independently of opportunity-kind filter. Store favorite opportunities and filters only in this browser, with storage-denied fallback. Export/import a bounded validated JSON file to transfer favorites/filters manually between devices. No automatic cloud sync or favorite-specific push claims. Show a small soon-closing favorites section without changing the requested default sort. Provide a separate pending-review view, not mixed into confirmed new counts.

## Delivery
Full installation ZIP with empty state and safe examples; existing-user update ZIP without data/, config.json, README.md or generated site/. Include the required workflow change explicitly and explain backing up locally customized workflows. No secrets, fonts, real claim history or demo records in installation state. Local tests, browser tests, and package merge tests; transparent live-network limitations.
