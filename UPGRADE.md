# v4 → v5: 수집 대상·제목·마감 일시·AI/데이터 필터 수정

기존 저장소와 ntfy/Pages 설정은 그대로 사용합니다. **운영 중인 저장소에는 전체 ZIP이 아니라 업데이트 ZIP을 적용**하세요.

## GitHub에서 적용

1. `inha-contest-alert-v5-update.zip`을 풀어 안쪽 파일/폴더를 기존 저장소의 같은 경로에 올립니다. `contest_alert/`, `web/`, `tests/`, `tools/`를 함께 교체하세요. ZIP 자체나 바깥 폴더 한 겹을 올리는 방식이 아닙니다.
2. `Commit changes`로 저장합니다. 본인이 수정한 코드/안내 문서는 먼저 백업 또는 병합하세요.
3. **Actions → Daily contest dashboard → Run workflow → send_today 체크하지 않음 → Run workflow**를 실행합니다.
4. `update`, `deploy`가 끝난 뒤 Pages를 새로고침합니다. 이전 화면이면 Ctrl+Shift+R / Mac Cmd+Shift+R로 강력 새로고침하세요.
5. 하단 수집 상태에서 `인하대학교 · 교내 공지`가 **직접 확인**으로 바뀌었는지 확인합니다. 해당 대표 홈페이지는 자동 수집에서 제외됩니다. 학과/센터/SW 게시판은 유지됩니다.
6. 센터 공고의 실제 제목, 상세정보의 AI·데이터 관련 근거, 확인된 마감 시각을 확인하세요. 신규 집계와 목록은 관련성을 확인한 공고만 사용합니다.

**파일 업로드나 페이지 방문만으로 재수집되지 않습니다. 3번 수동 실행을 해야 기존 기록에도 수정이 적용됩니다.**

## 보존되는 파일

업데이트 ZIP은 `data/`, `config.json`, `.github/`, 생성된 `site/`, `README.md`를 포함하지 않습니다. 기존 `data/state.json`을 지우거나 새 설치용 빈 파일로 덮어쓰지 마세요. 알림 시도 기록, 공고 ID, 최초 확인일, 일별 비교 기준을 유지합니다. README의 자동 표와 site는 다음 수집에서 갱신됩니다.

대표 홈페이지 제외 및 분야 필터는 **기존 config.json을 수정하지 않아도 코드의 새 기본값으로 적용**됩니다. `strict_ai_data`를 본인이 명시적으로 false로 설정했다면 분야 제한을 해제한 설정이므로 true로 바꾸세요. 기본 제공 설정에는 기존 사용자에게 그런 false 값이 없습니다.

## 바뀐 동작

### 1. 인하대 대표 홈페이지 제외
제공된 기본 출처 `id: inha`, `https://www.inha.ac.kr/`는 안정적인 자동 접근을 확인하지 못하여 자동 수집에서 제외했습니다. 이름/직접 확인 링크만 하단에 남고 실패 건수에는 포함하지 않습니다. 모든 인하대 공지를 없애는 것이 아니며 데이터사이언스학과·인공지능공학과·인공지능융합연구센터·SW사업단은 유지합니다.

### 2. 센터의 ‘공지사항’ 제목 복구
게시판명/페이지 메타 제목 대신 게시글 제목 영역과 목록 제목을 사용합니다. 이전 `detail_title`이 ‘공지사항’이면 무시하고 저장된 원래 목록 제목으로 복구합니다. 원래 제목도 없으면 다음 목록/상세 수집 때 복구하며, 일반 게시판 이름만으로 관련 공고라고 확정하지 않습니다.

### 3. 접수 일시
- `'26. 7. 16 ~ 8. 17`, 띄어 쓴 접수 항목명, 날짜가 먼저 나오는 표, 별도 마감 일시와 오전/오후 시각 지원.
- 실제 게시글의 form 안 본문이 삭제되는 문제 수정. 메뉴/이전글/추천글/취소선 날짜는 배제.
- 해당 공고의 명시적인 접수 시작·마감 공개 JSON/데이터 속성을 확인. 다른 공고의 날짜와 단순 행사 종료일은 신청 마감으로 쓰지 않음.
- 본문에 직접 연결된 공식 DACON/인공지능팩토리 접수처를 한 단계 보완 확인. 날짜만 찾고 시간이 없더라도 공식 일정 링크가 있으면 확인.
- 시간은 원문에 명시된 것만 표시. 날짜만 있는 공고에 23:59를 임의로 넣지 않음. 서로 충돌하는 시각은 확정하지 않음.
- 이미 저장된 공고도 파서 버전이 달라 다시 확인합니다. 기본 상세 요청 최대 30건, 추가 일정 최대 18회, 상세 브라우저 최대 16회, 상세 단계 360초의 소프트 시간 예산을 적용합니다. 기존 config의 명시적 예산을 우선합니다.

날짜·제목을 새로 확인한 기존 공고는 새 대회가 아닙니다. 최초 확인 ID를 유지하며 내용 변경으로 기록될 수 있습니다.

### 4. 관련 없는 공고 제외
공고 제목·주제·정제된 본문에 AI·데이터 관련 근거가 있어야 공개 목록과 알림에 표시됩니다. 기관명에 인공지능이 들어 있다는 이유, 사이트 공통 메뉴, ‘AI 작품 금지’ 문구만으로 관련 있다고 판단하지 않습니다. 학사/채용/수상 결과 안내도 제외합니다.

기존에 저장된 무관한 공고도 재분류해 숨깁니다. 아직 근거를 확인하지 못한 공고는 ‘관련성 확인 대기’로 숨기며 원본 ID는 보존합니다. 대시보드·CSV·README 표·알림·어제 대비 집계에 같은 표시 정책을 적용합니다. 출처별 상태에는 제외/대기 건수가 표시됩니다.

캠퍼스픽은 전 분야 플랫폼이므로 제목/카드의 AI·데이터 표현으로 1차 선별한 뒤 본문을 추가 확인합니다. **제목/카드에는 관련 표현이 없고 본문에만 있는 공고는 놓칠 수 있습니다.** DACON/인공지능팩토리의 대회 목록은 제목이 일반적인 과제여도 본문을 확인합니다. 생성형 AI 활용 영상·디자인 공모전은 AI 활용이 주제이면 포함합니다. AI 프로그램/교육도 모집·참여 성격이고 AI·데이터 주제이면 포함할 수 있습니다.

## 업데이트 첫날에 알아둘 것

목록 수가 줄어들 수 있습니다. 기존 공고가 삭제되거나 마감된 것으로 단정한 것이 아니라 분야 제외/관련성 확인 대기가 생기기 때문입니다. 날짜가 잘못되어 마감된 대회가 위에 있던 경우에는 올바른 날짜를 확인한 뒤 기본 목록에서 숨겨질 수 있습니다.

제목은 저장된 목록 원문으로 즉시 복구할 수 있지만, 날짜는 상세 요청 예산 안에서 순차적으로 다시 읽습니다. 남은 공고는 send_today를 끈 수동 실행을 더 하거나 다음 수집을 기다리세요. 이미 오늘 새 파서로 시도한 미확인 공고는 같은 날 반복 요청하지 않습니다.

## 한계와 검증 범위

실제 운영 저장소 URL/상태 파일은 제공되지 않아 사용자 데이터의 날짜 확인율을 측정하지 않았습니다. 실제 AIX 및 DACON 원문 텍스트를 공개 웹으로 확인했지만, 로컬 Python에서 사이트에 접속하는 종단간 검증·GitHub 배포·ntfy 실제 수신은 수행하지 못했습니다.

포스터 이미지/PDF/HWP에만 있는 날짜, 접근 제한, 동적 페이지 구조가 맞지 않는 경우는 여전히 미확인일 수 있습니다. OCR/유료 AI API/로그인·차단 우회는 추가하지 않았습니다. 근거 없는 날짜를 넣거나 수집 실패를 신규 0건으로 바꾸지 않습니다.

무료 구성, 기존 디자인, 긴 제목 2줄 표시, 마감 여유순, 어제 대비 신규, 정오 1회 알림은 유지합니다. 정시 수신이나 모든 공고의 완전한 수집은 보장하지 않습니다. 검증 상세는 `docs/TEST_REPORT.md`를 참조하세요.

## 처음 설치
전체 `inha-contest-alert-v5.zip`과 `SETUP.md`를 사용합니다. 전체 ZIP의 state.json은 신규 설치용 빈 상태입니다. 미리보기는 가상 데이터이며 실제 사이트 데이터가 아닙니다.
