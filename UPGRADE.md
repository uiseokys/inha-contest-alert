# v3 → v4 업데이트: 날짜·제목·정렬 개선

이미 운영 중인 저장소를 새로 만들지 마세요. ntfy 토픽, Secret, Pages, 정오 일정은 그대로 사용합니다.

## 적용 순서

1. `inha-contest-alert-v4-update.zip`을 풀고 안쪽 파일/폴더를 저장소의 같은 위치에 올립니다. ZIP 자체나 바깥 폴더 한 겹을 올리지 않습니다. `contest_alert/`, `web/`, `tests/`, `tools/`는 함께 교체하세요.
2. 업데이트 ZIP에는 **`data/`, `config.json`, `.github/`, 생성된 `site/`가 없습니다.** 기존 `data/state.json`은 반드시 보존합니다. 이 파일을 빈 파일로 덮어쓰거나 삭제하면 이전 공고와 발송 기록을 잃습니다.
3. GitHub에서 `Commit changes`로 저장합니다. 직접 수정한 Python/CSS/안내 문서는 먼저 백업·병합하세요. 업데이트 ZIP은 사용자 README를 덮어쓰지 않습니다.
4. **Actions → Daily contest dashboard → Run workflow → send_today 체크하지 않음 → Run workflow**로 수집 및 화면 갱신을 실행합니다.
5. `update`와 `deploy` 작업이 끝나면 Pages를 새로고침합니다. 기존 화면이 보이면 강력 새로고침(Ctrl+Shift+R / Mac Cmd+Shift+R)을 사용합니다.
6. 기본 정렬에 **마감 여유순**이 표시되는지, 출처별 수집 상태에 **접수 시작·마감 확인 / 일부 확인 / 미확인** 건수가 생기는지 확인합니다. 상세정보에서 **날짜 근거(원문)**와 근거 페이지 링크를 확인합니다.

## 다시 수집해야 날짜가 바뀝니다

파일만 교체하거나 페이지에 접속하는 것은 재수집이 아닙니다. 위의 수동 실행을 해야 저장된 날짜·제목이 업데이트됩니다. v4는 최근에 확인한 v3 공고도 새 날짜 파서의 재확인 대상으로 넣습니다. 기존 ID와 최초 발견일, 일별 알림 기록은 유지합니다.

원문 개수/실행시간/추가 요청 예산이 있으므로 많은 기존 공고를 **한 번에 전부 다시 읽지는 않습니다.** 첫 실행 후 남아 있다면 send_today를 끈 수동 실행을 다시 하거나 다음 날 수집을 기다립니다. 이미 v4로 시도한 미확인 공고는 같은 날 반복하지 않고 다음 날 재확인합니다. 아직 v4로 시도하지 않은 공고는 다음 수동 실행에도 대상이 됩니다.

`config.json`의 기존 `max_detail_requests: 30`은 그대로 존중합니다. 데이터 파일을 지워서 강제 재수집하지 마세요. 접속 차단·날짜가 이미지/PDF/HWP에만 있는 경우는 재실행 횟수를 늘려도 이 버전에서 해결되지 않을 수 있습니다.

## 변경 내용

- 참가 기간 / 응모·공모기간 / 접수 시작일·종료일 / 괄호·요일·공백·명시된 제목 연도에 따른 생략형 날짜 지원.
- 실제 요청에서는 데이콘의 `overview/schedule`을 개요 주소와 구분하고, 공개된 같은 공고 일정 링크를 한 단계 추가 확인.
- 주최기관만 찾아도 끝내지 않고 날짜 누락 시 제한된 브라우저 확인. 원문에 없는 날짜를 지어내지 않음.
- 원문 제목에서 플랫폼명·카운터·상태/태그를 분리. 두 줄 카드와 상세 전체 제목, 공통 접수 시작/마감 칸.
- 웹페이지 및 GitHub 자동 요약표: 미래 마감일 내림차순 → 미확인 → 이미 마감. ‘마감 임박순’과 ‘최신순’도 선택 가능.
- 기존 정오 알림 및 어제 대비 신규 비교는 변경하지 않음. 날짜를 새로 확인하거나 제목을 정정한 기존 공고는 ‘신규 공고’가 아니라 내용 변경으로 기록될 수 있음.

## 처음 설치

처음 설치할 때만 전체 `inha-contest-alert-v4.zip`을 사용하고 `SETUP.md`를 따릅니다. 전체 ZIP은 빈 state.json을 포함한 **새 설치용**입니다.

## 확인되지 않은 것

현재 사용자의 실제 GitHub 사이트 주소·실제 state.json은 제공받지 않았습니다. 이번 수정은 이전에 제공한 v3를 기반으로 하며, 실제 운영 데이터의 날짜 확인율/수집 성공률을 측정한 것은 아닙니다. 코드 테스트 결과와 실제 원격 배포 성공은 구분해야 합니다.
