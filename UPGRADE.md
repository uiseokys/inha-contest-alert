# v5 → v6: 실제 운영 사이트 점검 반영

대상: https://uiseokys.github.io/inha-contest-alert/ 및 해당 공개 GitHub 저장소.
2026-09-22 공개 원문에서 확인한 문제를 수정했습니다. 이 파일을 내려받는 것만으로 운영 사이트가 바뀌지는 않습니다. 이 작업에서 GitHub에 커밋하거나 실제 알림을 보내지는 않았습니다.

## 적용 순서

1. `inha-contest-alert-v6-update.zip`을 풉니다. 안쪽 `inha-contest-alert` 폴더의 **내용물**을 기존 저장소의 같은 위치에 업로드합니다. ZIP 자체나 바깥 폴더를 한 겹 더 올리지 않습니다.
2. `contest_alert/`, `web/`, `tests/`, `tools/`를 함께 교체하고 `Commit changes`로 저장합니다. 직접 수정한 Python·디자인·문서는 먼저 백업하거나 병합합니다.
3. **Actions → Daily contest dashboard → Run workflow → send_today 체크하지 않음 → Run workflow**를 실행합니다.
4. `update`와 `deploy`가 끝나면 Pages를 강력 새로고침합니다. 하단에 **CONTEST MONITOR · v6**, 필터에 **대회·프로그램 전체**가 보이는지 확인합니다.
5. 출처별 수집 상태에서 날짜 확인 수, 상세 조회 대기, 렌더링/일정 페이지 오류를 확인합니다. 첫 실행에서 대기 항목이 남을 수 있습니다. 날짜 복구는 해당 원문에 실제 접근할 수 있어야 하며 모든 항목을 한 번에 다시 읽는 방식이 아닙니다.

**기존 `data/state.json`을 지우거나 새 설치용 빈 파일로 덮어쓰지 마세요.** 업데이트 ZIP에는 `data/`, `config.json`, `.github/`, 생성된 `site/`, `README.md`가 없습니다. ntfy 토픽·개인 Secret·정오 시간·Pages 설정은 그대로 사용합니다. 수집 실행 시 코드가 기존 기록을 읽어 정리한 뒤 새 상태와 화면을 생성합니다.

## 실제로 달라지는 것

- 같은 K2web 게시글의 `?layout=unknown` 차이는 동일 공고로 처리합니다. 기존 ID는 삭제하지 않고 보조 ID로 보관합니다. 알림 발송 기록과 전날 비교 기록도 보존하며 URL 정리만으로 신규 공고가 생기지 않도록 합니다.
- `참여자 선발 결과`, `시험 결과 제출 및 장학금 신청` 등 사후 행정 공지는 새 참여 기회 목록·알림에서 제외합니다. 실제 AI·데이터 프로그램은 유지하며 공모전/프로그램 필터로 구분합니다.
- 공고 제목에 연도와 신청기간이 명시되어 있으면 본문에서 날짜를 놓쳤을 때 근거로 사용합니다. 기존 AI-POT 4차 신청(9/10~9/16)과 해외 인턴십(~8/16)은 제목의 2026년을 근거로 기간이 지난 항목으로 분류됩니다. 오늘 연도를 임의로 넣지 않습니다.
- DACON은 동일 대회 번호의 공식 `overview/schedule`을 추가 확인합니다. AF는 `참가 접수` 라벨과 일정표를 읽고 팀 병합·대회 종료·서류 제출과 구분합니다.
- 인공지능팩토리의 주최기관 칸에 들어간 책임·약관 문장과 상금 칸의 라이선스 문장을 정리합니다. 실제 주최/주관 라벨을 우선하고 잘못된 값을 새로 만들지 않습니다.
- 공개 플랫폼의 상세정보가 첫 article 바깥에 있어도 읽으며, 브라우저에서는 접수 일정이 렌더링될 때까지 제한적으로 기다립니다.
- 상세 조회 예산으로 미뤄진 항목, 일정 페이지 오류, 렌더링 오류를 출처 상태에 구분합니다. 목록 수집 정상과 마감일 추출 성공은 다른 지표입니다.
- 마감 여유순·두 줄 제목·정오 알림·어제 대비 신규 비교는 유지합니다.

## 기존 기록과 새 비교

중복 기록 및 제외 공고는 state에 남습니다. 공개 화면에서만 숨깁니다. 날짜·설명 정정은 기존 공고의 변경일 수 있으나 신규 대회로 만들지 않습니다. 같은 대회가 서로 다른 학과/플랫폼에 재게시된 것을 의미 기반으로 모두 통합하는 기능은 아닙니다.

## 한계

사이트와 공개 JSON 및 코드, 공식 일정 원문은 웹 도구로 읽었습니다. 로컬에서 선택한 실제 사례를 재현해 검사했지만 컨테이너 DNS 제한으로 실제 Python 수집기→GitHub Actions→Pages→ntfy 전체 흐름은 검증하지 못했습니다. 일부 캠퍼스픽 상세는 공개 웹 읽기에서도 제목 외 본문을 얻지 못했습니다. 접근 제한·이미지/PDF/HWP에만 있는 날짜는 계속 미확인으로 남을 수 있습니다.

`tools/preview_audit_v6.py`는 선택한 실제 사례와 공식 일정의 짧은 텍스트를 **재현용 HTML**로 구성합니다. 전체 운영 state나 원본 HTML을 그대로 다운로드한 파일이 아니며, 이 미리보기를 실사용 `site/index.html`로 덮어쓰면 안 됩니다.

처음 설치하는 경우에만 전체 v6 ZIP을 사용합니다. 기존 사용자는 위 업데이트 ZIP을 사용하세요.
