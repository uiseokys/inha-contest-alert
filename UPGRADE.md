# v2 → v3 업데이트

v3는 첨부 디자인 지침을 적용한 화면과 **어제 대비 신규 공고 수**를 추가합니다. 인하대·캠퍼스픽 수집, 기간/자격/링크 표시, 일일 11:40 수집·12:00 ntfy 알림은 유지합니다.

## 이미 v2를 올렸다면
1. `inha-contest-alert-v3-update.zip`을 풉니다. 안쪽 파일을 기존 저장소의 **같은 위치**에 덮어씁니다. ZIP 자체나 바깥 폴더를 올리지 않습니다.
2. 업데이트 묶음에는 `data/`, `config.json`, `.github/`, 생성된 `site/`가 없습니다. **기존 `data/state.json`을 삭제하거나 전체 ZIP의 빈 파일로 덮어쓰지 마세요.** 수집 기록·날짜별 알림 시도가 들어 있습니다.
3. 본인이 수정한 README/문서가 있으면 해당 파일은 병합하거나 덮어쓰기 전에 백업합니다. Python 코드·web/·새 테스트 파일은 함께 교체해야 합니다.
4. Actions → Daily contest dashboard → Run workflow에서 **send_today를 체크하지 않고** 실행합니다. 수집·화면·README를 갱신하고 새 일별 기준을 저장하며, 추가 ntfy 알림은 보내지 않습니다.
5. GitHub Pages를 새로고침합니다. 첫 v3 실행은 ‘비교 기준 준비 중’이 정상입니다. 다음 한국 날짜의 수집부터 전날과 비교합니다.
6. 수집 상태를 확인합니다. ntfy 토픽·Secret·Pages 설정을 새로 만들 필요가 없습니다.

## 처음 설치한다면
전체 `inha-contest-alert-v3.zip`을 사용하고 `SETUP.md`를 따릅니다. 전체 ZIP의 data/state.json은 **새 설치용 빈 상태**입니다. 미리보기 공고는 가상 데이터이며 배포 상태에 포함하지 않습니다.

## 신규 건수의 정확한 의미
기준은 **한국시간 어제의 마지막 저장 목록 → 오늘 최근 수집 목록**입니다. 알림이 성공했는지와 관계없이 수집 기록끼리 비교합니다. 같은 날 다시 수집해도 어제 기록을 바꾸지 않습니다. 어제 저녁 수동 갱신을 했다면 그 최종 기록이 기준이 됩니다.

새로 확인한 공고 주소를 세며, 실제 게시일이나 고유 대회 수를 확정하는 지표가 아닙니다. 같은 대회가 학과 게시판과 캠퍼스픽에 각각 올라오면 별도 공고로 셉니다. 마감된 공고가 기본 목록에서 숨겨져도 신규 건수에서 빼지 않습니다. 신규 요약 버튼은 이 차이를 피하도록 전체 상태의 신규 목록을 엽니다.

- 처음 설치 / v3 최초 업데이트: **비교 준비 중**, 기존 전체 목록을 신규로 세지 않음.
- 전날 수집 누락: 최근 저장 날짜를 **명시**해 비교하고 ‘어제 대비’라고 표현하지 않음.
- 모든 출처 수집 실패: **비교 불가**, 0건으로 단정하지 않음.
- 새 출처 최초 적재: 신규와 별도 표시.
- 전날 수집이 불완전했던 출처의 추가 확인: 신규 확정 집계에서 제외하고 별도로 표시.
- 일부 출처 실패: 확인 가능한 범위의 숫자와 경고 표시.

`daily_snapshots`는 기존 state.json에 자동으로 추가되며 최대 32일을 보관합니다. 초기화·수동 편집은 필요하지 않습니다. 정오 알림의 **‘지난 알림 이후 미전달 신규’는 별도 지표**입니다. 어제 저녁 수동 수집이나 과거 알림 실패가 있으면 어제 대비 수와 다를 수 있습니다.

## 그대로인 한계
이번 변경은 수집기의 실제 접속 성공을 새로 검증한 것이 아닙니다. 외부 게시판 구조·접근 제한·robots 정책, GitHub 예약 지연, ntfy/휴대폰 상태의 영향을 받습니다. 날짜별 최대 1회 제출 시도 정책을 그대로 유지합니다.
