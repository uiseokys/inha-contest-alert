# v6 → v7 업데이트 — 1~4순위 개선

대상: 기존 `inha-contest-alert` 저장소. **새 저장소·ntfy 토픽·Pages 설정을 만들지 않습니다.** 이 파일은 적용 안내이며 원격 배포 완료 확인서가 아닙니다.

## 이번에는 `.github`도 업데이트해야 합니다

이전 업데이트와 달리 **`.github/workflows/daily.yml`이 변경**되며, **`.github/workflows/diagnose.yml`이 추가**됩니다. 알림 결과와 실패 기록을 웹페이지까지 반영하고 읽기 전용 진단 실행을 제공하기 위해서입니다. Python·웹 파일만 올리면 이 부분은 적용되지 않습니다.

## 적용 순서

1. `inha-contest-alert-v7-update.zip`을 풉니다. 안쪽 `inha-contest-alert` 폴더의 **내용물**을 기존 저장소의 같은 위치에 업로드합니다. ZIP 자체나 바깥 폴더 한 겹을 올리지 않습니다.
2. **`.github` 숨김 폴더까지 포함**해 올립니다. Mac Finder의 `Command + Shift + .`로 표시할 수 있습니다. 웹 업로드에서 빠지면 GitHub의 `Add file → Create new file`로 아래 두 경로를 각각 만들고 제공한 파일 내용을 전부 붙여 넣습니다. 기존 `daily.yml`은 편집해서 전체 내용을 교체합니다.
   - `.github/workflows/daily.yml`
   - `.github/workflows/diagnose.yml`
3. 본인이 직접 수정한 코드·안내 문서·workflow는 먼저 백업하거나 변경 내용을 병합합니다. `daily.yml`의 사용자 변경은 자동 보존되지 않습니다. 기본 cron `40 2 * * *`와 `send_today`, 기존 Secret 이름은 유지했습니다.
4. `Commit changes`로 저장한 뒤 **Actions → Daily contest dashboard → Run workflow → `send_today` 체크하지 않음**으로 실행합니다. 이는 목록·화면 갱신이며 시험 알림을 추가하지 않습니다.
5. `update`와 `deploy`를 확인하고 Pages를 강력 새로고침합니다. 하단에 **CONTEST MONITOR · v7**, 실행 상태 영역에 **7.0.0 / 코드 지문 / 수집 시각**, 검색 조건에 **기술 분야 / 관심만**이 보이는지 확인합니다.
6. 날짜 진단에서 이번에 읽은 상세 수와 마감 확인 수, 미확인 원인을 확인합니다. 이전 날짜를 유지한 공고와 이번 실행에서 새로 확인한 공고는 구분합니다. 첫 재수집에서 모든 기존 상세가 갱신되지는 않을 수 있습니다.

## 반드시 보존할 파일

업데이트 ZIP에는 **`data/`, `config.json`, `README.md`, 생성된 `site/`, 활성 `overrides.json`이 없습니다.** 기존 `data/state.json`을 빈 파일로 덮어쓰거나 삭제하지 마세요. 공고 ID·알림 시도·전날 비교 기록이 들어 있습니다. 코드가 기존 상태에 필요한 필드를 추가하며 자동 초기화하지 않습니다.

`README.md`는 업로드 과정에서 덮어쓰지 않지만 이후 정상 수집 시 기존 프로그램의 자동 요약표 영역이 갱신됩니다. `.github/`는 이번에 바뀌는 파일이므로 위의 보존 목록에 포함되지 않습니다.

## 실제 GitHub 환경의 날짜 진단

**Actions → Audit date collection (no notification) → Run workflow**를 실행합니다. 이 별도 작업은 `contents: read` 권한으로, 기존 저장 상태의 복사본을 사용해 제한된 원문을 다시 읽습니다. **공고 상태를 저장하거나 ntfy 알림을 보내지 않습니다.**

완료된 실행의 Summary와 Artifacts의 **`date-quality-audit`**에서 `quality.md`, `quality.json`을 확인합니다. 조회한 출처·이번 상세 시도·날짜 확인·접근/렌더링/파싱/예산 문제를 구분합니다. 전체 사이트의 누락률을 보증하는 전수 검사는 아닙니다. 의존성 설치 단계부터 실패해 보고서가 없다면 해당 실행 로그를 먼저 확인합니다.

## 4가지 개선의 사용법

- **시간과 상태:** 마감 시각이 확인되면 그 시각을 기준으로 마감 처리합니다. 날짜만 있으면 ‘오늘 마감 · 시각 미확인’으로 표시하며 23:59를 만들지 않습니다. 페이지를 열어둔 동안 상태를 갱신하되 키보드로 읽고 있는 카드를 갑자기 제거하지 않습니다.
- **실행 진단:** 저장된 실행 버전/코드 지문/수집·생성 시각과 ntfy 접수 결과를 봅니다. 공개 GitHub 실행 상태는 인증 없이 한 번 추가 확인하며 실패하면 미확인으로 표시합니다. ntfy의 ‘수락’은 휴대폰 수신 성공이 아닙니다.
- **대회 단위 보기:** 공식 대회 주소 등 확실한 근거가 있는 공고만 통합합니다. 카드 상세에서 여러 출처 링크를 함께 봅니다. 새 공고 주소 수와 새 대회·프로그램 수, 재게시 공고를 구분합니다. 전날 비교 기록이 없거나 날짜가 지났으면 그 이유/기준 날짜를 표시합니다.
- **개인 탐색:** 기술 분야와 행사 종류를 따로 선택합니다. 카드의 ‘관심 저장’, ‘관심만’, ‘관심·설정 내보내기/가져오기’를 사용합니다. 저장은 **현재 브라우저**에 한정됩니다. 다른 기기에는 JSON 파일로 직접 옮깁니다. 브라우저 저장이 막히면 메모리에서만 유지하고 경고합니다.

관심 목록은 공개 저장소·ntfy 서버에 업로드되지 않습니다. 따라서 **관심 전용 ntfy 알림이나 기기 간 자동 동기화는 구현하지 않았습니다.** 페이지 상단의 관심 마감 임박 영역은 현재 기기에 저장한 관심 공고만 표시합니다. 기본 목록 정렬은 계속 마감 여유순입니다.

중요한 날짜를 원문에서 직접 확인한 경우 [날짜 보정 안내](docs/DATE_CORRECTIONS.md)를 참고하세요. 관련성이 애매한 공고는 별도 확인 대기 영역에서 원문으로 확인할 수 있습니다. 웹페이지에서 저장소의 공고 포함/제외 설정을 쓰는 관리자 기능은 아닙니다.

## 한계와 확인 책임

개발 환경에서 실제 GitHub 실행·배포·ntfy 수신은 하지 않았습니다. 공개 v6 데이터 관찰과 로컬 재현 테스트는 v7의 실제 수집 확인율을 뜻하지 않습니다. PDF·HWP·포스터 OCR은 추가하지 않았으며 접근 차단은 우회하지 않습니다. 새로운 날짜·마감 연장도 원문을 확인하세요. 자동으로 판정한 같은 대회 관계는 보수적이어서 일부 중복이 남을 수 있습니다.

처음 설치할 때만 전체 v7 ZIP을 사용하고 `SETUP.md`를 따릅니다. 전체 ZIP의 빈 state는 신규 설치용입니다.
